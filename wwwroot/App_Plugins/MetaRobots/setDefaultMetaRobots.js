(function () {

    console.log("Custom JavaScript is loaded!");


    //// Event listener for content type selection
    //const contentTypeSelector = document.querySelector('.content-type-selector');
    //if (contentTypeSelector) {
    //    contentTypeSelector.addEventListener('change', function () {
    //        const selectedContentType = this.value;

    //        // Check if the selected content type matches your criteria
    //        if (selectedContentType === 'yourPageTypeAlias') {
    //            // Set the default value for the metaRobots property
    //            const metaRobotsInput = document.querySelector('[data-property-alias="metaRobots"]');
    //            if (metaRobotsInput && !metaRobotsInput.value) {
    //                metaRobotsInput.value = "INDEX, FOLLOW"; // Set the default value
    //            }
    //        }
    //    });
    //}
})();